<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootswatch/4.4.1/minty/bootstrap.min.css">
  </head>
  <body>
    <div class="container">
      <h1>Bonjour cher Administrateur, une nouvelle commande vient d'être passée sur Kitten Pixtore !</h1>
      <h4>
        Voici les différents articles qui viennent d'être achetés pour un total de :
        <div class="btn btn-small btn-outline-info"><%= @order.user.cart.total_price %></div>
      </h4>
      <% @order.items.each do |line| %>
        <%= line.title %> : <%= line.price %> €
      <% end %>
      <p>
        Les informations de l'acheteur sont les suivantes :<br>
        <strong>Email : </strong><%= @order.user.email %><br>
        <strong>Profil crée le : </strong><%= @order.user.created_at %><br>
        <strong>Commande numéro : </strong><%= @order.id %>
      </p>
      <h4>A bientôt cher Administrateur !</h4>
    </div>
  </body>
</html>
