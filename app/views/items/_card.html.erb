<div class="col-4">
  <div class="card mb-5 mr-5" style="max-width: 20rem;">
    <div class="card-img"><img src= <%= item.image_url %> style="height: 250px; width: 100%; display: block;"></div>
      <div class="card-body">
        <h4 class="card-title"><%= item.title %></h4>
        <p class="card-text mb-1">Prix : <%= item.price %>€</p>
        <div class="btn-group mt-3" role="group" aria-label="Basic example">
        <!--Modif le javascript par le show_path-->
          <%= link_to "+ d'infos", item_path(item), class: "btn btn-outline-info btn-sm" %>
          <%= link_to "Ajouter au panier", cart_path(item), method: :put, class: "btn btn-success font-weight-bold mr-2 mb-2" %>
        </div>
      </div>
    </div>
</div>